# Article Retrieval — Kudremukh (4× RTX 6000 Ada, 47 GB VRAM each, 112 cores, 1 TB RAM).
# Run: python scripts/03_Article_retrieval/run_all.py --config configs/article_retrieval/kudremukh.yaml
# Inherits all experiment dimensions from base.yaml. Only compute settings differ.
base: "base.yaml"

domains: ["money-heist"]

parallel:
  n_workers: 16          # 16 workers — 1 per physical core group
  embedding_batch_size: 512  # RTX 6000 Ada (47 GB) handles 512 comfortably
